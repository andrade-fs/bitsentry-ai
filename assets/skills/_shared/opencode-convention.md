# OpenCode Convention (Contract)

This phase does not change OpenCode internals.

Conventions:
- skills/flow artifacts are referenced by path and summary
- OpenCode apply remains on existing safe path
- capability reports stay in `~/.bitsentry-ai/exports/capabilities/apply/`
- `latest.yaml` is the canonical latest pointer
